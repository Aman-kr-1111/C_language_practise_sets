// Input height and base if(height > base)then calculate area of triangle otherwise area of rectangle.
#include <stdio.h>
int main()
{
    // float base, height;
    // printf("Input a base : ");
    // scanf("%f", &base);
    // printf("Input a height : ");
    // scanf("%f", &height);
    // if (height > base)
    // {
    //     printf("The area of traingle is %.2f", (height * base) / 2);
    // }
    // else
    // {
    //     printf("The area of rectangle is %.2f", height * base);
    // }

    // return 0;
}