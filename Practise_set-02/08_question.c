// Input price of a shirt, number of shirts and discount,calculate the payable amount. 
#include<stdio.h>
int main()
{
    // float price,qty,dis,payable_amount;
    // printf("Enter the price of a shirt : ");
    // scanf("%f",&price); 
    // printf("Enter the discount of a shirt : ");
    // scanf("%f",&dis); 
    // printf("Enter the quantity of shirts : ");
    // scanf("%f",&qty); 
    // payable_amount = price*qty - (price*qty*dis)/100;
    // printf("The price of %.2f shirts is : %.2f",qty,payable_amount);

    // return 0;
}