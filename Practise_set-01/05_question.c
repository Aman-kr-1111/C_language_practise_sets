// Input height and base and calculate the area of traingle and rectangle 

#include<stdio.h>
int main()
{
    // float b,h;
    // printf("Enter of height : ");
    // scanf("%f",&h);
    // printf("Enter of base : ");
    // scanf("%f",&b);

    // printf("Area of rectangle is = %.2f\n",(b*h));
    // printf("Area of triangle is = %.2f\n",(b*h)/2);

    return 0;
}